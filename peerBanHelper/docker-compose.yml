services:
  peerbanhelper:
    image: ghostchu/peerbanhelper:v9.0.10
    container_name: peerbanhelper
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    ports:
      - "9898:9898"
    environment:
      - PUID=0
      - PGID=0
      - TZ=Asia/Shanghai
